{
  "name": "@chartalk/app-core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run src",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@chartalk/content-schema": "1.0.0",
    "@chartalk/dialogue-engine": "1.0.0",
    "@chartalk/sync-protocol": "1.0.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@chartalk/test-fixtures": "1.0.0"
  }
}
